nodes:
  - address: 40.71.254.68
    internal_address: 10.0.0.5
    user: azureuser
    role: [controlplane, worker, etcd]
    ssh_key_path: ./id_rsa

ingress:
  provider: nginx
  options:
    use-forwarded-headers: 'true'
